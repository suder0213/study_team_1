server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/likelion_book
    username: root
    password: 0428
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # 테이블 자동 생성
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 시간대 설정 (MySQL 서버와 일치하게)
  jackson:
    time-zone: Asia/Seoul